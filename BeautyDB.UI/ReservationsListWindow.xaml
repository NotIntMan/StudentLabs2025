<Window x:Class="BeautyDB.UI.ReservationsListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:BeautyDB.UI"
        mc:Ignorable="d"
        Title="Список записей" Height="600" Width="1000">
    <Window.DataContext>
        <local:ReservationsListWindowState/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Записи клиентов" FontSize="24" FontWeight="Bold" Margin="10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="Создать" Width="100" Height="30" Margin="0,0,10,0" Click="CreateButton_Click"/>
            <Button Content="Редактировать" Width="120" Height="30" Margin="0,0,10,0" Click="EditButton_Click"/>
            <Button Content="Удалить" Width="100" Height="30" Click="DeleteButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  Name="ReservationsDataGrid"
                  ItemsSource="{Binding Reservations}"
                  SelectedItem="{Binding SelectedReservation}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Мастер" Binding="{Binding Master}" Width="150"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="100"/>
                <DataGridTextColumn Header="Время" Binding="{Binding StartTime}" Width="80"/>
                <DataGridTextColumn Header="Длительность" Binding="{Binding Duration}" Width="100"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Client.Name}" Width="150"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Client.Phone}" Width="120"/>
                <DataGridTextColumn Header="Услуга" Binding="{Binding ServiceDescription}" Width="*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
